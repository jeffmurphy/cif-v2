SUBDIRS = src
docdir = doc
dist_doc_DATA = README

clean-local:
	rm -f *~

realclean: clean-local
	rm Makefile.in
	rm configure
	rm aclocal.m4
	rm -fr autom4te.cache
	rm config.log
	rm config.status
	rm config.h
	rm stamp-h1
	rm Makefile
